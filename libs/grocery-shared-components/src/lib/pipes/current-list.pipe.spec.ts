import { CurrentListPipe } from './current-list.pipe';

describe('CurrentListPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentListPipe();
    expect(pipe).toBeTruthy();
  });
});
