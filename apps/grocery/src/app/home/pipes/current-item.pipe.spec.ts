import { CurrentItemPipe } from './current-item.pipe';

describe('CurrentItemPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentItemPipe();
    expect(pipe).toBeTruthy();
  });
});
